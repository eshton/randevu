{
  "name": "@randevu/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Randevu CLI — `randevu start` opens a session on the hosted relay; `randevu start local` self-hosts a blind relay + tunnel.",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "randevu": "./dist/bin/randevu.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --coverage --coverage.provider=v8 --coverage.reporter=text --coverage.include=src/**",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/hashes": "^1.7.1",
    "@randevu/core": "workspace:*",
    "@randevu/local": "workspace:*"
  },
  "devDependencies": {
    "@randevu/relay": "workspace:*",
    "@randevu/relay-client": "workspace:*",
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
