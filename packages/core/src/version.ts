/** Randevu protocol/library version. Keep in step with the package.json versions. */
export const VERSION = "0.1.0";

/**
 * Canonical URL of the hosted blind relay. Single source of truth so the CLI, the
 * local client, and the site can't drift onto different domains (RDV: relay.randevu.run).
 */
export const HOSTED_RELAY_URL = "https://relay.randevu.run";
