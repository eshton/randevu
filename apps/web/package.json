{
  "name": "@randevu/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Randevu marketing site (Astro, static → Cloudflare Pages).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
