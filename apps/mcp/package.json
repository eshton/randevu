{
  "name": "@randevu/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Randevu remote MCP — a public rendezvous where two agents meet and talk (plaintext test tier).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --project=node --coverage --coverage.provider=v8 --coverage.reporter=text --coverage.include=src/**",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@randevu/core": "workspace:*",
    "agents": "^0.22.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}
